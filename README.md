# codex-relay

? WSL ??? Codex CLI ??????? Python ???????????? URL ? API Key??? Codex ????????????????????

## ??

- ? `codex-relay` ???????????
- ????????? `~/.codex/config.toml`?`~/.codex/auth.json` ??????
- ??????? `~/.codex/relays.json`???????? `600`?
- `current` ??? API Key ?? 8 ??? 4 ??????????????
- ?????? API Key?`.gitignore` ?????????? Codex ?????

## ????

- Ubuntu???? Linux?WSL ???
- `python3`?
- ?????? `PATH` ??? `codex` ???

?????

```sh
command -v python3
command -v codex
printf '%s\n' "$HOME"
test -d ~/.codex && echo '~/.codex exists'
test -f ~/.codex/config.toml && echo '~/.codex/config.toml exists'
```

## ??

? WSL ????????????

```sh
./install-wsl.sh
```

??????????

1. ????? `~/.local/bin/codex-relay` ??? `700`?
2. ????? `~/.codex/config.toml`??????????????????
3. ?? `~/.codex/relays.json` ?????????????? `600`?
4. ? `~/.bashrc` ????? `~/.local/bin` ? `PATH` ???
5. ?? `python3 -m py_compile`?`command -v codex-relay` ? `codex-relay help` ?????

???????? WSL ??????????? shell ???

```sh
export PATH="$HOME/.local/bin:$PATH"
```

## ??

```sh
codex-relay add <??> <URL> <API_KEY> [???]
codex-relay list
codex-relay use <??>
codex-relay current
codex-relay sync
```

???? `<API_KEY>` ??????????????? API Key ?????README?????? Git ???

## WSL ??????

### Windows ??? WSL ????

Windows ? `D:\\wengqian\\gitlab_project\\sd_new` ? WSL ??? `/mnt/d/wengqian/gitlab_project/sd_new`???????? WSL ??? Linux HOME??? `/home/boshi/.local/bin/codex-relay`???? Windows ?????

### ?? WSL ??????

????????????? Ubuntu?

```powershell
wsl.exe -d Ubuntu -- /bin/sh -c 'whoami; pwd'
```

????? Ubuntu ? WSL 2??? `/bin/sh` ???????????

### ????????? `source ~/.bashrc`

`.bashrc` ???? Conda ????Docker ??????????? Bash ??????????????? PATH ????????????????????????? `export PATH=...`?

### ????? Codex ??

?? `~/.codex/config.toml` ??????????????????????

```toml
model_provider = "custom"
model = "placeholder"
model_catalog_json = "cc-switch-model-catalog.json"

[model_providers.custom]
name = "placeholder"
base_url = "https://placeholder.invalid"
wire_api = "responses"
requires_openai_auth = true
```

### ?????

API Key ?? `~/.codex/relays.json` ??????? `600`??????? `700`???? `~/.codex`?`auth.json`?`relays.json` ?????????? Git?

## ????

```sh
python3 -m py_compile ~/.local/bin/codex-relay
command -v codex-relay
codex-relay help
```
